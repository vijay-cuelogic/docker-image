node {

    stage ('Clone') {
      checkout scm
    }
    stage ('Build') {
         sh 'gem install bundler'
        // sh bundle install
        // sh bundle exec rake test

        
        
    }
    stage ('Tests') {

    }

}