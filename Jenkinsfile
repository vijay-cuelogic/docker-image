node {

    stage ('Clone') {
      checkout scm
    }
    stage ('Build') {
         sh 'ls'
         sh 'ruby -v'
         sh 'bundle install'
        // sh bundle exec rake test

        
        
    }
    stage ('Tests') {

    }

}