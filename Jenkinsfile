node {

    stage ('Clone') {
      checkout scm
    }
    stage ('Build') {
         sh 'ls'
         //sh 'gem install bundler'
        // sh bundle install
        // sh bundle exec rake test

        
        
    }
    stage ('Tests') {

    }

}